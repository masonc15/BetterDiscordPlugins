{
  "name": "betterdiscord-move-server-to-top-plugin",
  "version": "0.1.0",
  "private": true,
  "description": "BetterDiscord plugin to move a server to the top from guild context menu.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^3.1.4"
  }
}
